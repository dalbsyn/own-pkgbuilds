# This is an example PKGBUILD file. Use this as a start to creating your own,
# and remove these comments. For more information, see 'man PKGBUILD'.
# NOTE: Please fill out the license field for your package! If it is unknown,
# then please put 'unknown'.

# Maintainer: Your Name <youremail@domain.com>
pkgname=local-wallpapers
pkgver=1
pkgrel=2
pkgdesc="System-wide installation of own wallpapers"
arch=(any)
license=('unknown')


package() {
	mkdir -p $pkgdir/usr/share/backgrounds/custom/
	mkdir -p $pkgdir $pkgdir/usr/share/gnome-background-properties/
	cd "$srcdir/backgrounds/"
	install * $pkgdir/usr/share/backgrounds/custom/
	cd "$srcdir/gnome-background-properties/"
	install * $pkgdir/usr/share/gnome-background-properties/
}
